'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('appTool').
  component('appTool', {
    templateUrl: 'app-tool/app-tool.template.html'
  });